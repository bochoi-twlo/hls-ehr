{
  "name": "hls-ehr",
  "version": "1.2.1",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "twilio": "3.76.0",
    "configure-env": "2.0.0-1",
    "node-fetch": "2.6.7"
  },
  "devDependencies": {},
  "engines": {
    "node": "14"
  }
}
